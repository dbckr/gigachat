{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "(Windows) Launch",
          "type": "lldb", // lldb || cppvsdbg
          "request": "launch",
          "program": "${workspaceRoot}/target/debug/gigachat.exe",
          "args": [],
          "stopAtEntry": false,
          "cwd": "${workspaceFolder}",
          "preLaunchTask": "cargo build",
          "environment": [{"name": "RUST_BACKTRACE", "value": "1"}],
      }
  ]
}