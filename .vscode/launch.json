{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "(Windows) Launch",
          "type": "lldb",
          "request": "launch",
          "program": "${workspaceRoot}/target/debug/gigachat.exe",
          "args": [],
          "stopAtEntry": false,
          "cwd": "${workspaceFolder}",
          "environment": [],
          "preLaunchTask": "cargo build"
      }
  ]
}